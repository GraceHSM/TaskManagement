<div class='container-fluid'>
  <nav class='topbar d-flex'>
    <%= link_to t('new_task'), new_task_path, class: 'rounded-pill bg-dark text-light px-4 py-2 mr-2' %>
    <%= link_to t('TaskLists'), root_path, class: 'rounded-pill bg-dark text-light px-4 py-2 mr-2' %>
    <%= link_to t('custom_sort'), list_tasks_path, class: 'rounded-pill bg-dark text-light px-4 py-2 mr-2' %>

    <% if is_admin?(@current_user) %>
      <%= link_to t('new_user'), new_user_path, class: 'rounded-pill bg-primary text-light px-4 py-2 mr-2' %>
      <%= link_to t('UserLists'), users_path, class: 'rounded-pill bg-primary text-light px-4 py-2' %>
    <% end %>
  </nav>
</div>
